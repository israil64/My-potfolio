import styled from "styled-components";

const ProjectsSection = styled.section`
  padding: 4rem 2rem;
  text-align: center;
`;

const ProjectContainer = styled.div`
  padding: 18px 7px;
  overflow: hidden;
  min-height: "100vh";
`;

const Project = styled.div`
  margin: 2rem 0;
`;

export { ProjectsSection, Project, ProjectContainer };
